export type ModalType = 'success' | 'failed';

export interface Theme {
  background: string;
  text: string;
  subText: string;
  inputBorder: string;
  primary: string;
  success: string;
  danger: string;
  warning: string;
  modalBg: string;
  overlay: string;
  timerBg: string;
}
